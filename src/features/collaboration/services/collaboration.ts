import { Router } from 'express';

/**
 * Collaboration API Routes - Placeholder for feature-based architecture
 * This is a simplified version to ensure compilation during migration
 */

const router = Router();

// Placeholder routes - to be fully implemented after migration is complete
router.get('/status', (req, res) => {
  res.json({
    status: 'success',
    message: 'Collaboration service is running',
    feature: 'collaboration'
  });
});

export default router;