{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://disco-mcp.up.railway.app/contracts/browserbase/navigate.request.json",
  "title": "browserbase.navigate.request",
  "description": "Request schema for browser navigation",
  "type": "object",
  "additionalProperties": false,
  "required": ["url"],
  "properties": {
    "url": {
      "type": "string",
      "format": "uri",
      "description": "URL to navigate to"
    },
    "waitUntil": {
      "type": "string",
      "enum": ["load", "domcontentloaded", "networkidle0", "networkidle2"],
      "default": "load",
      "description": "When to consider navigation complete"
    },
    "timeout": {
      "type": "integer",
      "minimum": 0,
      "maximum": 60000,
      "default": 30000,
      "description": "Navigation timeout in milliseconds"
    },
    "viewport": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "width": {
          "type": "integer",
          "minimum": 320,
          "maximum": 3840,
          "default": 1280
        },
        "height": {
          "type": "integer",
          "minimum": 240,
          "maximum": 2160,
          "default": 720
        }
      },
      "description": "Browser viewport dimensions"
    }
  }
}
