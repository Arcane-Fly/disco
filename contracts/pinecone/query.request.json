{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://disco-mcp.up.railway.app/contracts/pinecone/query.request.json",
  "title": "pinecone.query.request",
  "description": "Request schema for querying vectors from Pinecone",
  "type": "object",
  "additionalProperties": false,
  "required": ["vector", "topK"],
  "properties": {
    "namespace": {
      "type": "string",
      "description": "The namespace to query vectors from"
    },
    "vector": {
      "type": "array",
      "items": {
        "type": "number"
      },
      "minItems": 8,
      "description": "Query vector to find similar vectors"
    },
    "topK": {
      "type": "integer",
      "minimum": 1,
      "maximum": 10000,
      "description": "Number of top similar vectors to return"
    },
    "filter": {
      "type": "object",
      "additionalProperties": true,
      "description": "Metadata filter for query results"
    },
    "includeMetadata": {
      "type": "boolean",
      "default": false,
      "description": "Whether to include metadata in results"
    },
    "includeValues": {
      "type": "boolean",
      "default": false,
      "description": "Whether to include vector values in results"
    }
  }
}
